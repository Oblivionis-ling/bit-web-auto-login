# 更新记录

## v1.1 — 2026-07-31

- 将经过 48 小时稳定运行验证的监控与认证逻辑作为正式 v1.1。
- 新增 `Install.ps1` 一键安装/升级入口，部署到 `%LOCALAPPDATA%\BITWebAutoLogin`。
- 新增可直接双击的 `Install.cmd` Windows 安装入口。
- 一键安装器自动复用或创建 DPAPI 凭据、迁移 v1.0 任务、注册稳定任务名并启动验证。
- 新增 `Uninstall.ps1` 安全卸载入口，默认保留设置、日志和加密凭据。
- 测试脚本集中保留在 `tests/`，完整验收文档移动到 `docs/`。
- 诊断脚本自动识别正式安装目录与旧版任务。
- README 重写为完整下载、安装、升级、诊断、卸载和安全说明。

## v1.0 — 2026-07-21

- 初始版本。
- 支持 `Auto`、`Wifi`、`Ethernet` 三种连接模式。
- Wi‑Fi 只接受 `BIT-Web`；有线只接受活动的物理网卡和允许的 IPv4 前缀。
- 不切换或配置任何网络连接。
- 支持普通 HTML 登录表单自动识别、隐藏字段保留和同源保护。
- 支持 DPAPI 加密凭据、单次测试、后台轮询、指数退避和日志保留。
- 提供默认只预览的任务计划安装与卸载脚本。
- 添加完全离线的逻辑测试。

### 2026-07-24

- 默认连接模式改为 `Auto`，保留 Wi-Fi-only 选择并增加校园网有线识别。
- 凭据改为需要发起认证时才读取；公网已经正常时无需加载密码。
- 根据真实入口页增加深澜（Srun）challenge、HMAC-MD5、SHA-1 和 SRBX1 认证支持。
- 公网探测改用 Microsoft 标准 HTTP 探测地址，避开当前网络的 HTTPS 证书链问题。

### 2026-07-29

- 添加全面验收流程，覆盖认证恢复、错误网络保护、持续监控、睡眠唤醒、重启和 24 小时稳定性。
- 添加只读诊断脚本，用于检查连接资格、任务状态、监控进程和日志。
- 修复公网探测偶发失败导致的重复认证：增加双探测、连续失败确认、5 分钟认证冷却和登录后多次验证。
- 将认证错误的指数退避上限从 5 分钟调整为 30 分钟；达到上限后仍持续重试。
